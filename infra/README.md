# infra

Thu muc nay chua cac file deployment va local orchestration.

Hien tai repo da co:

- `docker-compose.yml` o root
- `.env.example` de tham khao env

Co the bo sung sau:

- nginx config
- production compose
- deployment notes

